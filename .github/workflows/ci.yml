name: CI

on:
  push:
    branches:
      - main
      - master
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - name: Configure Git Credentials
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e ".[dev,docs]"
      - run: python -m pytest -q
      - name: Verify installed CLI contract
        run: |
          chatnlp --version
          chatnlp --tree
          chatnlp --tree-brief
      - name: Verify typed ChatEnv provider
        run: |
          chatenv --home "$RUNNER_TEMP/chatnlp-chatenv" list
          chatenv --home "$RUNNER_TEMP/chatnlp-chatenv" test -t chatnlp -I
          chatenv --home "$RUNNER_TEMP/chatnlp-chatenv" new ci -t chatnlp -I --yes
      - run: python -m build
      - run: python -m twine check dist/*
      - name: Verify built wheel CLI and profile contracts
        run: |
          python -m venv "$RUNNER_TEMP/chatnlp-wheel"
          "$RUNNER_TEMP/chatnlp-wheel/bin/python" -m pip install dist/*.whl
          (
            cd "$RUNNER_TEMP"
            "$RUNNER_TEMP/chatnlp-wheel/bin/chatnlp" --version
            "$RUNNER_TEMP/chatnlp-wheel/bin/chatnlp" --tree
            "$RUNNER_TEMP/chatnlp-wheel/bin/chatnlp" --tree-brief
            "$RUNNER_TEMP/chatnlp-wheel/bin/chatenv" --home "$RUNNER_TEMP/chatnlp-wheel-home" list
            "$RUNNER_TEMP/chatnlp-wheel/bin/chatenv" --home "$RUNNER_TEMP/chatnlp-wheel-home" test -t chatnlp -I
            "$RUNNER_TEMP/chatnlp-wheel/bin/chatenv" --home "$RUNNER_TEMP/chatnlp-wheel-home" new ci -t chatnlp -I --yes
          )
      - run: mkdocs build --strict
